<%- include('header') %>

<main class="container">
    <h1>Lista de Artistas</h1>
    
    <% if (typeof artistas !== 'undefined' && artistas.length > 0) { %>
        <div class="lista-artistas">
            <% artistas.forEach(artista => { %>
                <div class="artista-card">
                    
                    <!-- Usamos el enlace directo guardado en la base de datos -->
                    <!-- Se incluye un 'onerror' por si el link falla o no carga -->
                    <img src="<%= artista.imagen_url %>" 
                         alt="<%= artista.nombre %>" 
                         onerror="this.src='/img/default.jpg';">
                    
                    <h3><%= artista.nombre %></h3>
                    <p><%= artista.biografia %></p>
                    <a href="/detalle/<%= artista.id %>">Ver más</a>
                </div>
            <% }) %>
        </div>
    <% } else { %>
        <p>No se encontraron artistas en la base de datos.</p>
    <% } %>
</main>

<%- include('footer') %>
